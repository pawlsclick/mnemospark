{
  "id": "mnemospark",
  "name": "mnemospark",
  "version": "0.1.0",
  "description": "OpenClaw plugin for mnemospark storage workflow with wallet management and x402 USDC payments.",
  "configSchema": {
    "type": "object",
    "properties": {
      "walletKey": {
        "type": "string",
        "description": "EVM wallet private key (0x...). Optional — auto-generated if not set for /wallet and /cloud flows."
      }
    }
  },
  "uiHints": {
    "walletKey": {
      "label": "Wallet Private Key",
      "sensitive": true,
      "placeholder": "0x... (optional — auto-generated)"
    }
  }
}
